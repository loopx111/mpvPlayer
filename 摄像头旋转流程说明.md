# 摄像头旋转流程说明

## 当前版本状态
- **版本**：d25a21d（优化摄像头分辨率和旋转逻辑）
- **摄像头分辨率**：480×640（竖屏模式）
- **UI显示**：竖屏布局，480×640

## 图像处理流程

### 1. 摄像头读取阶段
```
摄像头 → 原始帧(480×640竖屏) → 基础控制器
```
- 分辨率：480×640（竖屏）
- 无翻转或镜像操作

### 2. 检测器处理阶段
```
基础控制器 → 嵌入式控制器 → MediaPipe检测器
```
- **人脸检测**：在480×640竖屏图像上检测
- **数字绘制**：在检测器处理阶段完成
  - 位置：`x_min - 30`（人脸左侧，额头位置）
  - 角度：`text_angle = 90`（顺时针90度，数字竖直，头朝左）

### 3. 旋转显示阶段
```
检测器结果 → 顺时针旋转90度 → RGB转换 → UI显示
```
- **旋转操作**：`cv2.rotate(display_frame, cv2.ROTATE_90_CLOCKWISE)`
- **最终显示**：480×640竖屏，数字在额头上方，方向正确

## 关键参数设置

### 摄像头控制器 (`embedded_mediapipe_controller.py`)
```python
# 分辨率设置
self.cap.set(cv2.CAP_PROP_FRAME_WIDTH, 480)
self.cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 640)

# 旋转设置（第283行）
display_frame = cv2.rotate(display_frame, cv2.ROTATE_90_CLOCKWISE)
```

### 检测器数字绘制 (`embedded_mediapipe_detector.py`)
```python
def _calculate_number_position_and_angle(self, face_landmarks, w, h, x_min, y_min, x_max, y_max):
    # 位置：在人脸左侧（额头位置）
    text_x_pos = x_min - 30
    text_y_pos = face_center_y
    
    # 角度：顺时针90度（数字竖直，头朝左）
    text_angle = 90
    
    return text_x_pos, text_y_pos, text_angle
```

### UI布局设置 (`main_window.py`)
```python
# 摄像头显示区域尺寸（竖屏）
self.camera_display_area.setMinimumSize(480, 640)
```

## 旋转逻辑总结

### 1. 原始图像状态
- 分辨率：480×640（竖屏）
- 人脸方向：横着（额头在左侧）
- 数字绘制：在人脸左侧，顺时针90度旋转（竖直，头朝左）

### 2. 旋转变换
- **UI旋转**：顺时针90度
- **数字变换**：
  - 位置：从人脸左侧 → 额头上方
  - 方向：从竖直（头朝左）→ 竖直（头朝上）

### 3. 最终显示效果
- 图像：480×640竖屏显示
- 人脸：正确方向，额头在上方
- 数字：在额头上方，竖直显示，方向正确

## 已删除的多余操作

### ❌ 移除的基础垂直翻转
```python
# 之前（已注释）
# frame = cv2.flip(frame, 0)  # 垂直翻转
```

### ❌ 移除的水平镜像
```python
# 之前（已注释）
# frame_mirrored = cv2.flip(frame, 1)  # 水平镜像
```

## 性能优化

### 1. 检测器参数
```python
min_detection_confidence=0.5  # 降低检测阈值，提高小尺寸人脸识别
min_tracking_confidence=0.3   # 保持跟踪阈值
```

### 2. 移除的操作
- 减少不必要的翻转和镜像操作
- 简化图像处理流程
- 提高处理速度和准确性

## 开发注意事项

### 1. 新增功能时
- 保持现有的旋转逻辑不变
- 在检测器阶段处理所有图像变换
- 确保UI旋转后显示正确

### 2. 调试技巧
- 检查数字位置：应该在额头位置
- 检查数字方向：应该竖直显示
- 验证旋转后图像方向

## 相关文件

- `src/camera/embedded_mediapipe_controller.py` - 主要旋转逻辑
- `src/ai/embedded_mediapipe_detector.py` - 数字绘制逻辑
- `src/ui/main_window.py` - UI布局设置
- `src/player/camera_controller.py` - 基础摄像头控制

---
*最后更新：当前版本 d25a21d*