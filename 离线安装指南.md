# MPV Player 离线安装指南

## 📦 离线安装方案概述

为满足无网络环境的部署需求，提供了两种离线安装方案：

### 方案一：完整离线安装包（推荐）
- 包含所有Python依赖包的预下载版本
- 完全离线安装，无需网络连接
- 适合生产环境批量部署

### 方案二：最小化安装包
- 仅包含应用程序文件
- 依赖包需提前准备或通过其他方式获取
- 适合有内部软件源的环境

## 🚀 快速开始

### 创建离线安装包

```bash
# 在有网络的环境下运行此脚本
./create_offline_installer.sh
```

### 离线部署步骤

1. **传输安装包到目标机器**
   ```bash
   # 使用U盘、光盘或内部网络传输
   scp mpvplayer_offline_20250304.tar.gz user@target-machine:/tmp/
   ```

2. **在目标机器上安装**
   ```bash
   # 解压安装包
   cd /tmp
   tar -xzf mpvplayer_offline_20250304.tar.gz
   
   # 进入安装目录
   cd mpvplayer_offline_20250304/installer
   
   # 运行离线安装脚本
   sudo ./install_offline.sh
   ```

## 📁 安装包结构

```
mpvplayer_offline_20250304/
├── installer/                 # 应用程序文件
│   ├── src/                  # Python源代码
│   ├── data/                 # 配置文件和数据
│   ├── scripts/              # 启动脚本
│   ├── models/               # AI模型文件
│   ├── requirements.txt      # 依赖列表
│   ├── install_offline.sh    # 离线安装脚本
│   └── 麒麟系统安装指南.md    # 详细文档
├── packages/                 # Python依赖包（可选）
│   ├── paho_mqtt-1.6.1-py3-none-any.whl
│   ├── PySide6-6.6.3-cp38-abi3-manylinux_2_28_x86_64.whl
│   └── ... (其他依赖包)
└── 部署说明.txt              # 快速部署指南
```

## 🔧 无网络环境下的依赖管理

### 提前准备依赖包

```bash
# 在有网络的环境下下载所有依赖包
pip download -r requirements.txt -d offline_packages/

# 下载AI模块依赖
pip download -r requirements_ai.txt -d offline_packages/
```

### 使用内部软件源

如果环境有内部PyPI镜像源，可以配置pip使用：

```bash
# 创建pip配置文件
mkdir -p ~/.pip
cat > ~/.pip/pip.conf << EOF
[global]
index-url = http://internal-pypi.example.com/simple/
trusted-host = internal-pypi.example.com
EOF
```

### 系统包依赖

麒麟系统的基础依赖包可以通过以下方式获取：

1. **使用系统安装光盘**
   ```bash
   # 挂载安装光盘
   sudo mount /dev/cdrom /mnt/cdrom
   
   # 配置本地源
   sudo cp /etc/apt/sources.list /etc/apt/sources.list.backup
   sudo echo "deb file:/mnt/cdrom kylin main" > /etc/apt/sources.list
   sudo apt update
   ```

2. **使用内部APT镜像**
   ```bash
   # 配置内部APT源
   sudo tee /etc/apt/sources.list << EOF
   deb http://internal-apt.example.com/kylin kylin main
   EOF
   sudo apt update
   ```

## ⚙️ 安装脚本详解

### install_offline.sh 主要功能

```bash
#!/bin/bash
# 主要安装步骤：

# 1. 环境检查
- 检查Python3是否安装
- 检查MPV播放器
- 验证系统类型

# 2. 文件部署
- 创建安装目录 (/opt/mpvPlayer)
- 复制应用程序文件
- 设置文件权限

# 3. 依赖安装
- 创建Python虚拟环境
- 优先使用离线包安装
- 备用方案：在线安装（如果允许）

# 4. 系统配置
- 创建systemd服务
- 配置桌面快捷方式
- 设置数据目录

# 5. 权限设置
- 设置用户权限
- 配置摄像头访问权限
- 设置日志目录
```

## 🛠️ 定制化部署

### 修改配置文件

安装前可以修改 `installer/data/config_kylin.json`：

```json
{
  "mqtt": {
    "host": "192.168.1.100",    // 修改为实际MQTT服务器
    "port": 1883,
    "username": "your_username",
    "password": "your_password"
  },
  "player": {
    "videoPath": "/opt/mpvPlayer/data/videos"
  }
}
```

### 禁用特定功能

如果不需要AI功能，可以移除AI相关依赖：

```bash
# 编辑install_offline.sh，注释掉AI模块安装部分
# "$INSTALL_DIR/venv/bin/pip" install --no-index --find-links=../packages -r requirements_ai.txt
```

## 🐛 常见问题解决

### 依赖安装失败

**问题**: 离线包安装失败
**解决**: 
```bash
# 检查离线包完整性
ls -la ../packages/

# 手动安装单个包
"$INSTALL_DIR/venv/bin/pip" install --no-index --find-links=../packages package_name
```

### 权限问题

**问题**: 摄像头无法访问
**解决**: 
```bash
# 添加用户到video组
sudo usermod -a -G video $USER

# 重新登录或重启
sudo reboot
```

### 服务启动失败

**问题**: systemd服务无法启动
**解决**: 
```bash
# 查看服务日志
sudo journalctl -u mpvplayer.service -f

# 检查服务文件语法
sudo systemctl daemon-reload
sudo systemctl reset-failed mpvplayer.service
```

## 🔄 批量部署方案

### 使用Ansible进行批量部署

```yaml
# playbook.yml
- name: Deploy MPV Player
  hosts: display_nodes
  become: yes
  tasks:
    - name: Copy installer package
      copy:
        src: mpvplayer_offline_20250304.tar.gz
        dest: /tmp/
    
    - name: Extract package
      unarchive:
        src: /tmp/mpvplayer_offline_20250304.tar.gz
        dest: /tmp/
        remote_src: yes
    
    - name: Run installer
      command: /tmp/mpvplayer_offline_20250304/installer/install_offline.sh
      args:
        chdir: /tmp/mpvplayer_offline_20250304/installer
```

### 使用Shell脚本批量部署

```bash
#!/bin/bash
# deploy_multiple.sh

NODES=("node1" "node2" "node3")

for node in "${NODES[@]}"; do
    echo "Deploying to $node..."
    
    # 传输安装包
    scp mpvplayer_offline_20250304.tar.gz $node:/tmp/
    
    # 远程执行安装
    ssh $node "cd /tmp && \
               tar -xzf mpvplayer_offline_20250304.tar.gz && \
               cd mpvplayer_offline_20250304/installer && \
               sudo ./install_offline.sh"
    
    echo "Deployment to $node completed"
done
```

## 📊 验证安装

### 基本功能验证

```bash
# 测试视频播放
/opt/mpvPlayer/venv/bin/python -c "
import sys
sys.path.append('/opt/mpvPlayer/src')
from player.mpv_controller import MPVController
print('MPV控制器导入成功')
"

# 测试摄像头
/opt/mpvPlayer/venv/bin/python /opt/mpvPlayer/diagnose_camera.py

# 测试MQTT连接
/opt/mpvPlayer/venv/bin/python -c "
import paho.mqtt.client as mqtt
print('MQTT客户端导入成功')
"
```

### 服务状态检查

```bash
# 检查服务状态
sudo systemctl status mpvplayer.service

# 查看应用日志
tail -f /opt/mpvPlayer/data/logs/mpvPlayer.log

# 测试桌面快捷方式
/opt/mpvPlayer/start_kylin.sh
```

## 🔒 安全考虑

### 权限最小化

```bash
# 创建专用用户
sudo useradd -r -s /bin/false mpvplayer
sudo chown -R mpvplayer:mpvplayer /opt/mpvPlayer

# 限制服务权限
sudo tee /etc/systemd/system/mpvplayer.service << EOF
[Service]
User=mpvplayer
Group=mpvplayer
ReadWritePaths=/opt/mpvPlayer/data
ReadOnlyPaths=/opt/mpvPlayer/src /opt/mpvPlayer/venv
EOF
```

### 网络安全

```json
{
  "mqtt": {
    "host": "192.168.1.100",
    "port": 1883,
    "tls": true,
    "ca_certs": "/opt/mpvPlayer/data/ca.crt"
  }
}
```

---

**重要提示**: 离线安装前请确保：
1. 目标机器系统版本与安装包兼容
2. 有足够磁盘空间（至少1GB）
3. 具备必要的系统权限
4. 提前测试安装包在相似环境下的兼容性