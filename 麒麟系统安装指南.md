# MPV Player 麒麟系统安装指南

## 📋 安装前准备

> **⚠️ 重要提示**：项目**必须**安装到 `/opt/mpvPlayer` 目录。使用 systemd 服务启动时，该路径在 `mpvplayer.service` 中硬编码，不支持其他路径。

### 系统要求
- **麒麟系统版本**: Kylin V10 或更高版本
- **Python版本**: Python 3.8+
- **内存**: 至少 2GB RAM
- **存储**: 至少 1GB 可用空间

### 网络要求
- 需要访问互联网以下载依赖包
- 需要访问 MQTT 服务器（如果使用远程控制功能）

### 离线安装包准备（适用于无法访问互联网的环境）
- 从 Windows/Mac 下载项目源码后，使用 `create_offline_package.py` 脚本生成 `mpvplayer_kylin_offline.zip`
- 将该 zip 包上传到麒麟系统的 `~/Downloads/` 目录

## 🚀 快速安装（推荐）

### 方式一：一键安装脚本

```bash
# 1. 下载项目到麒麟系统
cd /opt
sudo git clone https://github.com/loopx111/mpvPlayer.git
cd mpvPlayer

# 2. 运行一键安装脚本（包含所有依赖和配置）
sudo ./install_kylin_deps_v2.sh

# 3. 启动应用
./start_kylin.sh
```

### 方式二：分步安装

```bash
# 1. 安装系统依赖
sudo apt update
sudo apt install -y python3 python3-pip python3-venv mpv v4l-utils libopencv-dev libgl1-mesa-glx

# 2. 创建项目目录
sudo mkdir -p /opt/mpvPlayer
sudo chown $USER:$USER /opt/mpvPlayer
cd /opt/mpvPlayer

# 3. 下载项目文件（从GitHub或通过scp传输）
git clone https://github.com/loopx111/mpvPlayer.git .

# 4. 设置虚拟环境
python3 -m venv venv
source venv/bin/activate

# 5. 安装Python依赖
pip install --upgrade pip
pip install -r requirements.txt

# 6. 设置执行权限
chmod +x start_kylin.sh
chmod +x install_kylin_deps_v2.sh

# 7. 启动应用
./start_kylin.sh
```

### 方式三：离线安装包安装（适用于无法访问互联网的环境）

```bash
# 1. 创建安装目录
sudo mkdir -p /opt/mpvPlayer

# 2. 将离线安装包（mpvplayer_kylin_offline.zip）上传到麒麟系统
#    假设上传到用户家目录 ~/Downloads/

# 3. 解压zip包到安装目录（zip内已是项目根目录，直接解压覆盖）
cd ~/Downloads
unzip -o mpvplayer_kylin_offline.zip -d /opt/mpvPlayer/

# 4. 设置目录权限和脚本执行权限
sudo chown -R $USER:$USER /opt/mpvPlayer
sudo chmod +x start_kylin.sh install_kylin_deps_v2.sh *.sh

# 5. 进入安装目录
cd /opt/mpvPlayer

# 6. 创建数据目录并设置权限
sudo mkdir -p /opt/mpvPlayer/data/downloads /opt/mpvPlayer/data/videos
sudo chmod -R 777 /opt/mpvPlayer/data/downloads /opt/mpvPlayer/data/videos

# 7. 安装系统依赖和Python依赖
sudo ./install_kylin_deps_v2.sh

# 8. 配置systemd服务（开机自启动）
sudo cp mpvplayer.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable mpvplayer.service

# 9. 启动服务
sudo systemctl start mpvplayer.service

# 10. 查看服务状态
sudo systemctl status mpvplayer.service
```

> **提示**：如果需要修改MQTT配置，可编辑 `data/config_kylin.json` 文件。

## 🔧 生产环境部署

### 创建系统服务（开机自启动）

```bash
# 1. 创建systemd服务文件
sudo tee /etc/systemd/system/mpvplayer.service > /dev/null <<EOF
[Unit]
Description=MPV Player for Advertisement Display
After=network.target graphical.target

[Service]
Type=simple
User=$USER
WorkingDirectory=/opt/mpvPlayer
Environment=DISPLAY=:0
Environment=QT_QPA_PLATFORM=xcb
ExecStart=/opt/mpvPlayer/venv/bin/python -m src.app -c data/config_kylin.json
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 2. 重新加载systemd并启用服务
sudo systemctl daemon-reload
sudo systemctl enable mpvplayer.service

# 3. 启动服务
sudo systemctl start mpvplayer.service

# 4. 查看服务状态
sudo systemctl status mpvplayer.service
```

### 创建桌面快捷方式

```bash
# 将桌面快捷方式文件复制到系统目录
sudo cp mpvplayer.desktop /usr/share/applications/
sudo desktop-file-install /usr/share/applications/mpvplayer.desktop
```

> **提示**：确保 `start_kylin.sh` 脚本存在于项目目录中。

## 📁 目录结构说明

```
/opt/mpvPlayer/
├── src/                    # Python源代码
├── data/                   # 配置和数据文件
│   ├── config_kylin.json   # 麒麟系统专用配置
│   ├── videos/             # 视频文件目录
│   ├── downloads/          # 下载文件目录
│   └── logs/               # 日志文件
├── venv/                   # Python虚拟环境
├── start_kylin.sh          # 启动脚本
├── install_kylin_deps_v2.sh # 依赖安装脚本
└── requirements.txt        # Python依赖列表
```

## ⚙️ 配置说明

### 主要配置文件：`data/config_kylin.json`

```json
{
  "mqtt": {
    "host": "192.168.30.55",
    "port": 1883,
    "username": "baicells",
    "password": "BlTf129",
    "enabled": true
  },
  "player": {
    "videoPath": "/opt/mpvPlayer/data/videos",
    "autoPlay": true,
    "loop": true,
    "volume": 70
  },
  "system": {
    "logLevel": "INFO",
    "autostart": false
  }
}
```

### 自定义配置步骤：

1. **修改MQTT服务器地址**：根据实际网络环境修改host字段
2. **调整视频路径**：确保videoPath指向正确的视频目录
3. **设置自动启动**：将autostart设为true启用开机自启动

## 🐛 故障排除

### 常见问题及解决方案

#### 1. 依赖安装失败
```bash
# 清理缓存并重试
sudo apt clean
sudo apt update --fix-missing

# 手动安装缺失的包
sudo apt install -y libxcb-xinerama0 libxcb-randr0 libxcb-xfixes0
```

#### 2. 视频无法显示
```bash
# 检查显示环境
echo $DISPLAY

# 检查图形驱动
sudo apt install -y mesa-utils
glxinfo | grep "OpenGL version"

# 检查MPV安装
mpv --version
```

#### 3. 摄像头无法使用
```bash
# 检查摄像头设备
ls -la /dev/video*

# 检查摄像头权限
sudo usermod -a -G video $USER

# 重启系统使权限生效
sudo reboot
```

#### 4. 服务启动失败
```bash
# 查看服务日志
sudo journalctl -u mpvplayer.service -f

# 检查服务状态
sudo systemctl status mpvplayer.service

# 重新启动服务
sudo systemctl restart mpvplayer.service
```

### 日志文件位置
- 应用日志：`/opt/mpvPlayer/data/logs/mpvPlayer.log`
- 系统日志：`/var/log/syslog`
- 服务日志：`sudo journalctl -u mpvplayer.service`

## 🔄 更新和维护

### 更新到最新版本
```bash
cd /opt/mpvPlayer
sudo git pull origin master
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart mpvplayer.service
```

### 备份配置和数据
```bash
# 备份配置和视频文件
sudo tar -czf mpvplayer_backup_$(date +%Y%m%d).tar.gz \
  /opt/mpvPlayer/data/config_kylin.json \
  /opt/mpvPlayer/data/videos/

# 恢复备份
sudo tar -xzf mpvplayer_backup_20250304.tar.gz -C /
```

## 📊 性能优化建议

### 麒麟系统专用优化

1. **禁用不必要的服务**：
   ```bash
   sudo systemctl disable bluetooth.service
   sudo systemctl disable cups.service
   ```

2. **优化内存使用**：
   ```bash
   # 添加swap空间（如果内存不足）
   sudo dd if=/dev/zero of=/swapfile bs=1M count=2048
   sudo mkswap /swapfile
   sudo swapon /swapfile
   ```

3. **图形性能优化**：
   ```bash
   # 安装图形驱动优化包
   sudo apt install -y mesa-vulkan-drivers
   ```

## 📞 技术支持

### 获取帮助
- 查看详细文档：`docs/` 目录
- 查看脚本说明：`scripts/README.md`
- 问题反馈：检查日志文件获取详细信息

### 紧急恢复
如果应用无法启动，可以尝试：
```bash
# 重置虚拟环境
cd /opt/mpvPlayer
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 重置配置文件
cp data/config_kylin.json.backup data/config_kylin.json
```

---

**注意**：本指南适用于标准麒麟系统环境，如果遇到特殊硬件或定制化系统，可能需要额外配置。